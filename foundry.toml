[profile.default]
src = "src"
out = "out"
libs = ["lib"]
test = "test"

# Compiler
solc = "0.8.28"
evm_version = "cancun"

# fs permissions
fs_permissions = [
  { access = "read", path = "data/" },
]
